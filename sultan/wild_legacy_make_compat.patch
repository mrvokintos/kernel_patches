diff --git a/google-modules/amplifiers/cs35l41/Makefile b/google-modules/amplifiers/cs35l41/Makefile
--- a/google-modules/amplifiers/cs35l41/Makefile
+++ b/google-modules/amplifiers/cs35l41/Makefile
@@ -13,2 +13,3 @@
+EXTRA_CFLAGS += -I$(srctree)/$(src)/include/
 EXTRA_CFLAGS += -I$(KERNEL_SRC)/$(M)/include/
 EXTRA_CFLAGS += -I$(ROOT_DIR)/private/google-modules/amplifiers/snd_soc_wm_adsp
diff --git a/google-modules/amplifiers/cs35l45/Makefile b/google-modules/amplifiers/cs35l45/Makefile
--- a/google-modules/amplifiers/cs35l45/Makefile
+++ b/google-modules/amplifiers/cs35l45/Makefile
@@ -17,1 +17,2 @@
+EXTRA_CFLAGS += -I$(srctree)/$(src)/include/
 EXTRA_CFLAGS += -I$(KERNEL_SRC)/$(M)/include/
diff --git a/google-modules/aoc/alsa/Makefile b/google-modules/aoc/alsa/Makefile
--- a/google-modules/aoc/alsa/Makefile
+++ b/google-modules/aoc/alsa/Makefile
@@ -10,2 +10,3 @@
+EXTRA_CFLAGS	+= -I$(srctree)/sound/usb
 EXTRA_CFLAGS	+= -I$(KERNEL_SRC)/sound/usb
 EXTRA_CFLAGS	+= -I$(ROOT_DIR)/private/google-modules/amplifiers/audiometrics
diff --git a/google-modules/aoc/alsa/aoc_alsa_usb.c b/google-modules/aoc/alsa/aoc_alsa_usb.c
index ad45d0a..934a36b 100644
--- a/google-modules/aoc/alsa/aoc_alsa_usb.c
+++ b/google-modules/aoc/alsa/aoc_alsa_usb.c
@@ -220,6 +220,5 @@ int aoc_usb_init(void)
 
 void aoc_usb_exit(void)
 {
-	unregister_trace_android_vh_audio_usb_offload_connect(audio_usb_offload_connect, NULL);
 }
 
diff --git a/google-modules/amplifiers/cs40l26/cs40l26-i2c.c b/google-modules/amplifiers/cs40l26/cs40l26-i2c.c
index 9582113..5e7531b 100644
--- a/google-modules/amplifiers/cs40l26/cs40l26-i2c.c
+++ b/google-modules/amplifiers/cs40l26/cs40l26-i2c.c
@@ -12,6 +12,8 @@
 
 #include "cs40l26.h"
 
+static int cs40l26_probed_retry_count[CS40L26_MAX_DEVICES];
+
 static const struct i2c_device_id cs40l26_id_i2c[] = {
 	{"cs40l26a", 0},
 	{"cs40l26b", 1},
diff --git a/google-modules/amplifiers/cs40l26/cs40l26.c b/google-modules/amplifiers/cs40l26/cs40l26.c
index 19243bf..d53b32a 100644
--- a/google-modules/amplifiers/cs40l26/cs40l26.c
+++ b/google-modules/amplifiers/cs40l26/cs40l26.c
@@ -677,7 +677,7 @@ static irqreturn_t cs40l26_handle_mbox_buffer(int irq, void *data)
 	irqreturn_t irq_status = IRQ_HANDLED;
 	struct device *dev = cs40l26->dev;
 	u32 val = 0;
-	int error;
+	int __maybe_unused error;
 
 	mutex_lock(&cs40l26->lock);
 
diff --git a/google-modules/amplifiers/cs40l26/cs40l26.h b/google-modules/amplifiers/cs40l26/cs40l26.h
index 8a07bd8..f00c7ad 100644
--- a/google-modules/amplifiers/cs40l26/cs40l26.h
+++ b/google-modules/amplifiers/cs40l26/cs40l26.h
@@ -1320,7 +1320,6 @@ int cs40l26_wseq_write(struct cs40l26_private *cs40l26, u32 addr, u32 data,
 int cs40l26_copy_f0_est_to_dvl(struct cs40l26_private *cs40l26);
 int cs40l26_rom_wt_init(struct cs40l26_private *cs40l26);
 
-static int cs40l26_probed_retry_count[CS40L26_MAX_DEVICES];
 void cs40l26_add_codec_devices(struct device *dev);
 
 /* external tables */
diff --git a/google-modules/bms/gbms_storage.c b/google-modules/bms/gbms_storage.c
index 670a9ef..20e9b12 100644
--- a/google-modules/bms/gbms_storage.c
+++ b/google-modules/bms/gbms_storage.c
@@ -551,8 +551,6 @@ int gbms_storage_offline(const char *name, bool flush)
 EXPORT_SYMBOL_GPL(gbms_storage_offline);
 
 /* ------------------------------------------------------------------------ */
-#ifdef CONFIG_DEBUG_FS
-
 static int gbms_storage_show_cache(struct seq_file *m, void *data)
 {
 	int bucket;
@@ -861,8 +859,6 @@ static ssize_t debug_export_tag(struct file *filp,
 
 GBMS_DEBUG_ATTRIBUTE(gbms_providers_export_ops, NULL, debug_export_tag);
 
-#endif
-
 /* ------------------------------------------------------------------------ */
 
 struct gbms_storage_device {
diff --git a/google-modules/bms/google_battery.c b/google-modules/bms/google_battery.c
index ce7cfbb..9412d91 100644
--- a/google-modules/bms/google_battery.c
+++ b/google-modules/bms/google_battery.c
@@ -7034,8 +7034,6 @@ static ssize_t charge_full_estimate_show(struct device *dev, struct device_attri
 
 static DEVICE_ATTR_RO(charge_full_estimate);
 
-#ifdef CONFIG_DEBUG_FS
-
 static int cycle_count_bins_store(void *data, u64 val)
 {
 	struct batt_drv *batt_drv = (struct batt_drv *)data;
@@ -7514,8 +7512,6 @@ static int debug_ravg_fops_write(void *data, u64 val)
 
 DEFINE_SIMPLE_ATTRIBUTE(debug_ravg_fops, NULL, debug_ravg_fops_write, "%llu\n");
 
-#endif
-
 /* ------------------------------------------------------------------------- */
 
 static ssize_t debug_get_fake_temp(struct file *filp,
diff --git a/google-modules/bms/google_bms.c b/google-modules/bms/google_bms.c
index 939075c..bd7ddb3 100644
--- a/google-modules/bms/google_bms.c
+++ b/google-modules/bms/google_bms.c
@@ -33,6 +33,31 @@
 #include "google_psy.h"
 #include "google_bms.h"
 
+/* the number should be the same as GBMS_AACT_NB_LIMITS_MAX */
+static aact_limits_profiles_t aact_all_limits = {
+    .temp_limits = {
+        "google,aact-temp-limits",
+        "google,aact-temp-limits-1",
+        "google,aact-temp-limits-2",
+        "google,aact-temp-limits-3",
+        "google,aact-temp-limits-4"
+    },
+    .cv_limits = {
+        "google,aact-cv-limits",
+        "google,aact-cv-limits-1",
+        "google,aact-cv-limits-2",
+        "google,aact-cv-limits-3",
+        "google,aact-cv-limits-4"
+    },
+    .cc_limits = {
+        "google,aact-cc-limits",
+        "google,aact-cc-limits-1",
+        "google,aact-cc-limits-2",
+        "google,aact-cc-limits-3",
+        "google,aact-cc-limits-4"
+    }
+};
+
 /* sync from google/logbuffer.c */
 #define LOG_BUFFER_ENTRY_SIZE   256
 
diff --git a/google-modules/bms/google_bms.h b/google-modules/bms/google_bms.h
index 571f87c..82e118f 100644
--- a/google-modules/bms/google_bms.h
+++ b/google-modules/bms/google_bms.h
@@ -148,31 +148,6 @@ typedef struct {
     char *cc_limits[GBMS_AACT_NB_LIMITS_MAX];
 } aact_limits_profiles_t;
 
-/* the number should be the same as GBMS_AACT_NB_LIMITS_MAX */
-static aact_limits_profiles_t aact_all_limits = {
-    .temp_limits = {
-        "google,aact-temp-limits",
-        "google,aact-temp-limits-1",
-        "google,aact-temp-limits-2",
-        "google,aact-temp-limits-3",
-        "google,aact-temp-limits-4"
-    },
-    .cv_limits = {
-        "google,aact-cv-limits",
-        "google,aact-cv-limits-1",
-        "google,aact-cv-limits-2",
-        "google,aact-cv-limits-3",
-        "google,aact-cv-limits-4"
-    },
-    .cc_limits = {
-        "google,aact-cc-limits",
-        "google,aact-cc-limits-1",
-        "google,aact-cc-limits-2",
-        "google,aact-cc-limits-3",
-        "google,aact-cc-limits-4"
-    }
-};
-
 #define WLC_BPP_THRESHOLD_UV	7000000
 #define WLC_EPP_THRESHOLD_UV	11000000
 
diff --git a/google-modules/bms/google_charger.c b/google-modules/bms/google_charger.c
index 5c45a2c..a743cec 100644
--- a/google-modules/bms/google_charger.c
+++ b/google-modules/bms/google_charger.c
@@ -35,10 +35,8 @@
 #include "google_dc_pps.h"
 #include "google_psy.h"
 
-#ifdef CONFIG_DEBUG_FS
 #include <linux/debugfs.h>
 #include <linux/seq_file.h>
-#endif
 
 #if IS_ENABLED(CONFIG_GOOGLE_CRASH_DEBUG_DUMP)
 #include <soc/google/google-cdd.h>
@@ -3856,8 +3854,6 @@ static int chg_vote_input_suspend(struct chg_drv *chg_drv,
 	return 0;
 }
 
-#ifdef CONFIG_DEBUG_FS
-
 static int chg_get_input_suspend(void *data, u64 *val)
 {
 	struct chg_drv *chg_drv = (struct chg_drv *)data;
@@ -4127,9 +4123,6 @@ static int bd_enabled_set(void *data, u64 val)
 DEFINE_SIMPLE_ATTRIBUTE(bd_enabled_fops, bd_enabled_get,
 			bd_enabled_set, "%lld\n");
 
-
-#endif
-
 static int debug_get_pps_cc_tolerance(void *data, u64 *val)
 {
 	struct chg_drv *chg_drv = data;
@@ -5467,8 +5460,6 @@ state2power_table_show(struct device *dev, struct device_attribute *attr, char *
 
 static DEVICE_ATTR_RO(state2power_table);
 
-#ifdef CONFIG_DEBUG_FS
-
 static ssize_t tm_store(struct chg_thermal_device *tdev,
 			const char __user *user_buf,
 			size_t count, loff_t *ppos)
@@ -5538,8 +5529,6 @@ static ssize_t dc_tm_store(struct file *filp, const char __user *user_buf,
 
 DEBUG_ATTRIBUTE_WO(dc_tm);
 
-#endif // CONFIG_DEBUG_FS
-
 static int chg_init_mdis_stats_map(struct chg_drv *chg_drv, const char *name) {
 	int rc, byte_len;
 
diff --git a/google-modules/bms/google_cpm.c b/google-modules/bms/google_cpm.c
index 124a9cc..5b44d00 100644
--- a/google-modules/bms/google_cpm.c
+++ b/google-modules/bms/google_cpm.c
@@ -3118,8 +3118,6 @@ static const struct thermal_cooling_device_ops chg_mdis_tcd_ops = {
 	.set_cur_state = gcpm_set_mdis_charge_cntl_limit,
 };
 
-#ifdef CONFIG_DEBUG_FS
-
 static ssize_t mdis_tm_store(struct file *filp, const char __user *user_buf,
 			     size_t count, loff_t *ppos)
 {
@@ -3360,9 +3358,6 @@ static int dc_cc_lim_store(void *data, u64 val)
 DEFINE_SIMPLE_ATTRIBUTE(dc_cc_lim_fops, dc_cc_lim_show,
 			dc_cc_lim_store, "%lld\n");
 
-
-#endif // CONFIG_DEBUG_FS
-
 /* ------------------------------------------------------------------------- */
 
 static int mdis_out_init_sel_online(u32 *out_sel, int len, const struct gcpm_drv *gcpm)
diff --git a/google-modules/bms/google_dc_pps.c b/google-modules/bms/google_dc_pps.c
index 5649d77..79b3d43 100644
--- a/google-modules/bms/google_dc_pps.c
+++ b/google-modules/bms/google_dc_pps.c
@@ -25,10 +25,8 @@
 #include "google_dc_pps.h"
 #include <linux/usb/max77759_export.h>
 
-#ifdef CONFIG_DEBUG_FS
 #include <linux/debugfs.h>
 #include <linux/seq_file.h>
-#endif
 
 #define PPS_UPDATE_DELAY_MS		2000
 
diff --git a/google-modules/display/common/include/gs_drm/gs_display_mode.h b/google-modules/display/common/include/gs_drm/gs_display_mode.h
index d279409..5b05571 100644
--- a/google-modules/display/common/include/gs_drm/gs_display_mode.h
+++ b/google-modules/display/common/include/gs_drm/gs_display_mode.h
@@ -93,7 +93,7 @@
  * This macro checks drm clock value is divisible by 1000, and returns (drm clock value/1000).
  */
 #define CALC_DRM_CLOCK_K_HZ(CLOCK) \
-	(CHECK_DRM_CLOCK_DIVISIBLE_1000(CLOCK) + (CLOCK / 1000))
+	(CLOCK / 1000)
 
 /**
  * DRM_MODE_TIMING() - fills in timing parameters in struct drm_display_mode
diff --git a/google-modules/soc/gs/Makefile.include b/google-modules/soc/gs/Makefile.include
--- a/google-modules/soc/gs/Makefile.include
+++ b/google-modules/soc/gs/Makefile.include
@@ -6,2 +6,5 @@
+subdir-ccflags-y += -I$(ROOT_DIR)/private/google-modules/soc/gs/include
+subdir-ccflags-y += -I$(ROOT_DIR)/private/google-modules/soc/gs/include/uapi
+
 KCFLAGS += -I$(ROOT_DIR)/private/google-modules/soc/gs/include
 KCFLAGS += -I$(ROOT_DIR)/private/google-modules/soc/gs/include/uapi
diff --git a/google-modules/soc/gs/drivers/cpufreq/exynos-acme.c b/google-modules/soc/gs/drivers/cpufreq/exynos-acme.c
index 88890a6..c8e5bf7 100644
--- a/google-modules/soc/gs/drivers/cpufreq/exynos-acme.c
+++ b/google-modules/soc/gs/drivers/cpufreq/exynos-acme.c
@@ -21,6 +21,7 @@
 #include <linux/pm_opp.h>
 #include <linux/suspend.h>
 #include <linux/platform_device.h>
+#include <linux/tensor_aio.h>
 
 #include <soc/google/cal-if.h>
 #include <soc/google/ect_parser.h>
@@ -121,6 +122,66 @@ static unsigned int get_freq(struct exynos_cpufreq_domain *domain)
 	return freq;
 }
 
+#ifdef CONFIG_ARM_TENSOR_AIO_DEVFREQ
+void exynos_acme_rate_info(struct exynos_cpufreq_domain *domain,
+			   struct exynos_acme_rate *r)
+{
+	lockdep_assert_irqs_disabled();
+
+	/* Copy out info about the last rate change for this domain */
+	read_lock(&domain->rate_info_lock);
+	*r = domain->rate_info;
+	read_unlock(&domain->rate_info_lock);
+}
+
+void exynos_acme_rate_latched(struct exynos_cpufreq_domain *domain,
+			      const struct exynos_acme_rate *cookie)
+{
+	lockdep_assert_irqs_disabled();
+
+	/* Clear out the transition info if the last target rate latched */
+	write_lock(&domain->rate_info_lock);
+	if (domain->rate_info.set_time == cookie->set_time &&
+	    domain->rate_info.freq == cookie->freq)
+		domain->rate_info.set_time = 0;
+	write_unlock(&domain->rate_info_lock);
+}
+
+static int domain_set_rate(struct exynos_cpufreq_domain *domain,
+			   unsigned long rate)
+{
+	unsigned long flags;
+	int ret;
+
+	/*
+	 * Set the new rate and update the transition info if it succeeds. This
+	 * is done with IRQs disabled so that an interrupt cannot fire after
+	 * cal_dfs_set_rate() and mess up the recorded transition time.
+	 */
+	local_irq_save(flags);
+	ret = cal_dfs_set_rate(domain->cal_id, rate);
+	if (!ret) {
+		u64 now = __arch_counter_get_cntpct();
+
+		write_lock(&domain->rate_info_lock);
+		/* Only update the time if the previous rate switch latched */
+		if (!domain->rate_info.set_time)
+			domain->rate_info.set_time = now;
+		domain->rate_info.freq = rate;
+		write_unlock(&domain->rate_info_lock);
+	}
+	local_irq_restore(flags);
+
+	return ret;
+}
+#else
+static int domain_set_rate(struct exynos_cpufreq_domain *domain,
+			   unsigned long rate)
+{
+	return cal_dfs_set_rate(domain->cal_id, rate);
+}
+#endif
+
 static int set_freq(struct exynos_cpufreq_domain *domain,
 		    unsigned int target_freq)
 {
@@ -132,7 +193,7 @@ static int set_freq(struct exynos_cpufreq_domain *domain,
 	if (domain->need_awake)
 		disable_power_mode(cpumask_any(&domain->cpus), POWERMODE_TYPE_CLUSTER);
 
-	err = cal_dfs_set_rate(domain->cal_id, target_freq);
+	err = domain_set_rate(domain, target_freq);
 	if (err < 0) {
 		pr_err("failed to scale frequency of domain%d (%d -> %d)\n",
 		       domain->id, domain->old, target_freq);
@@ -198,11 +259,19 @@ static void apply_thermal_pressure(struct exynos_cpufreq_domain *domain,
 	cpumask_t *maskp;
 	unsigned long arch_capped_freq;
 
-	if (!domain || (domain->capped_freq[thermal_actor] == capped_freq))
+	if (!domain)
 		return;
 
 	maskp = &domain->cpus;
 
+	if (IS_ENABLED(CONFIG_ARM_TENSOR_AIO_DEVFREQ)) {
+		tensor_aio_cpufreq_pressure(cpumask_any(maskp), capped_freq);
+		return;
+	}
+
+	if (domain->capped_freq[thermal_actor] == capped_freq)
+		return;
+
 	spin_lock(&domain->thermal_update_lock);
 	domain->capped_freq[thermal_actor] = capped_freq;
 	arch_capped_freq = min(domain->capped_freq[TJ], domain->capped_freq[TSKIN]);
@@ -280,6 +349,13 @@ static int exynos_cpufreq_init(struct cpufreq_policy *policy)
 	policy->dvfs_possible_from_any_cpu = true;
 	cpumask_copy(policy->cpus, &domain->cpus);
 
+#ifdef CONFIG_ARM_TENSOR_AIO_DEVFREQ
+	/* rate_info_lock would need to be a raw lock on RT */
+	BUILD_BUG_ON(IS_ENABLED(CONFIG_PREEMPT_RT));
+	rwlock_init(&domain->rate_info_lock);
+	tensor_aio_init_cpu_domain(domain);
+#endif
+
 	pr_info("CPUFREQ domain%d registered\n", domain->id);
 
 	return 0;
diff --git a/google-modules/soc/gs/drivers/cpufreq/exynos-acme.h b/google-modules/soc/gs/drivers/cpufreq/exynos-acme.h
index 7a077f3..b18e96d 100644
--- a/google-modules/soc/gs/drivers/cpufreq/exynos-acme.h
+++ b/google-modules/soc/gs/drivers/cpufreq/exynos-acme.h
@@ -37,6 +37,19 @@ struct exynos_cpufreq_file_operations {
 	unsigned int			default_value;
 };
 
+#ifdef CONFIG_ARM_TENSOR_AIO_DEVFREQ
+struct exynos_acme_rate {
+	u64 set_time;
+	unsigned int freq;
+};
+
+struct exynos_cpufreq_domain;
+void exynos_acme_rate_info(struct exynos_cpufreq_domain *domain,
+			   struct exynos_acme_rate *r);
+void exynos_acme_rate_latched(struct exynos_cpufreq_domain *domain,
+			      const struct exynos_acme_rate *cookie);
+#endif
+
 enum thermal_actors {
 	TJ = 0,
 	TSKIN = 1
@@ -97,6 +110,11 @@ struct exynos_cpufreq_domain {
 	unsigned long capped_freq[NR_THERMAL_ACTORS];
 	char capped_freq_name[NR_THERMAL_ACTORS][THERMAL_PRESSURE_STR_LEN];
 	unsigned int ect_table_offset;
+
+#ifdef CONFIG_ARM_TENSOR_AIO_DEVFREQ
+	struct exynos_acme_rate rate_info;
+	rwlock_t rate_info_lock;
+#endif
 };
 
 /*
diff --git a/google-modules/soc/gs/drivers/watchdog/s3c2410_wdt.c b/google-modules/soc/gs/drivers/watchdog/s3c2410_wdt.c
index 62b4dae..184af6f 100644
--- a/google-modules/soc/gs/drivers/watchdog/s3c2410_wdt.c
+++ b/google-modules/soc/gs/drivers/watchdog/s3c2410_wdt.c
@@ -1916,7 +1916,6 @@ static int s3c2410wdt_remove(struct platform_device *dev)
 	unregister_pm_notifier(&s3c2410wdt_pm_nb);
 
 	if (wdt->schedstat) {
-		WARN_ON(unregister_trace_android_vh_scheduler_tick(vh_scheduler_tick, wdt));
 		free_percpu(wdt->schedstat);
 	}
 
